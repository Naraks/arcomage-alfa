## Тикет

ARC-XXX — <!-- название тикета из dev_plan_tickets.md -->

## Что сделано

<!-- Кратко: что и почему, а не как — детали "как" видны из диффа. -->

## Чеклист

- [ ] Заголовок PR и все коммиты внутри ссылаются на один и тот же `ARC-XXX` (см. `CONTRIBUTING.md`).
- [ ] Критерии приёмки тикета в `dev_plan_tickets.md` выполнены и отмечены галочками (или явно указано, что реализовано частично, и почему).
- [ ] Тесты добавлены/обновлены, если PR меняет логику `match_manager.gd`/`artifact_manager.gd` (см. ARC-053, ARC-072/073).
- [ ] Ветка собирается и запускается в редакторе Godot хотя бы один раз перед мержем.
- [ ] `pre-commit run --all-files` пройден (gdformat/gdlint) — см. ARC-062.

## Как проверить

<!-- Шаги для ревьюера: что открыть в редакторе, какой сценарий прогнать. -->
